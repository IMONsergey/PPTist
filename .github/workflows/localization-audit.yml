name: Russian localization audit (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-auto-audit-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic localization audit disabled to avoid recursive PR synchronization. Residual audits run inside controlled localization workflows."
