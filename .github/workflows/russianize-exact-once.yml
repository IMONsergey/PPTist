name: Russianize PPTist UI — exact v1 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived after successful build/type-check validation; v2 handles normalized commit output."
