name: Russianize PPTist UI (legacy manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-legacy-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy localization workflow disabled. Use russianize-exact-once.yml for controlled localization passes."
